Now execute everything in the project_def2.md file. Ensure you handle and create everything needed and if possible reuse what we already have. This is a production app so ensure we store all the information in Filecoin, we encrypted it. We also create the logic to analyze it and create our sentiment cards for the coins and tweet groups. We call the api once per day and no get more than 50 tweets per day, 12 per each group (4 groups so far). All user login is handled via Clerk, all sensitive data from users encrypt it before sending to filecoin. Ensure we upload only to the github repot the prescient folder, no anything else. And also we don't commit/update the keys or any sensitive field. Ensure you don't have errors and you deliver an app ready to upload to vercelr or railway, so it's ready to production environment. 