We'll change the narrative and focus of our project. Our project is gonna be based on creating an information HUB of social platforms. A place where we remove the noise and tackle the problem of so many informtion available. 

Now, specific to this synthesis.md hackathons, we need to a first MVP where we we are going to compete for is any of the following options:

b. Agents for Public Goods Data Analysis, $1,000:  Agents that extract patterns from messy public goods data (especially qualitative) that humans can't scale to.
c. Agents for Public Goods Data Collection, $1,000: Agents that gather richer signals about a project's real-world impact and legitimacy.

Now, as we have defined what we want to do, I need you to ensure that if we build an app where we get data from X using the X API. We use the API to get posts from specific accounts and then with those accounts, posts, interactions with posts and analysis we classify them in groups. So, let's put it like this:
User Julian registers to the app, information is stored in Filecoin. 
Then, user Julian is able to see all the groups of Tweeters that we have: 
    CryptoTweets: @Zeneca, @intocryptoverse, @IncomeSharks, @Nebraskangooner
    StockTweets: @arny_trezzi, @amitisinvesting
    TechTweets: @andrewchen,@skominers, @jason
    GeopoliticsTweets: @TuckerCarlson, @DonMiami3
So the previous groups were made considering: Quality of the tweets information, frequence of posting, interactions of people with their posts, specific topics or keywords managed in their tweets. 
Now, we got Tweets (user, content, total of likes, Total of comments, total of reposts, user total of likes, user registration date) from them each day, ensuring we don't get mor than 50 tweets per day (this because of the cost of each API usage). Once we got the tweets, we should be able to store them in Filecoin to ensure we win the following prize:
@Filecoin - $2,000
Best Use Case with Agentic Storage - $2,000:
Agents that use Filecoin Onchain Cloud as mission-critical, autonomous storage and data infrastructure with real mainnet usage.

Before storing each tweet into Filecoin, ensure we encryot them using Lighthouse SDK + Kavach Encryption so no one can access to the tweet itself on Filecoing but can see the tweet info through the app.

So once this is stored in Filecoin, we must be able to create the Pythong Code to analyze, get keywords, understand what the tweet means and get a final conclusion of the group of tweets generated by each one of the member of each group so we get (an overview score from 1 to 10, a sentimente signal (bullisn, neutral, bearish), a summary of information obtained from the web, total number of tweets used, total number of accounts that tweeted.). Also ensure that we are going to use data from Dune Analytics API, and add this information below as small cards (Market Cap, value, mentions in the tweets that we go from the groups (for example, if Bitcoin/BTC was mentioned in 20 tweets, we must include in the Bitcoin card 20), virality score (based on the sentiment analysis, if people is talking about it this should be high)). For this small cards, consider for now the top 10 excluding stablecoins. 

Now, imagine we do this daily, we should be able to see historic analysis, so we can keep tracking how was the behavior of each group one week ago an be able to get our own conclusions. 

The main app will have in the frontend:
- Dashboard (where we can see the groups and everything described in line 23)
- History (page where we can filter by month/year/week to see all history of the group tweets)
- Info (an explanation of how we conclude the Dashboard summary and signal. This must be done in a technical way so we can proof that what we determined is enough and with strong fundamentals)
- Suggest Person (an option to sent us as a company possible accounts to integrate)
